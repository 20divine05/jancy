{
  "name": "dropshield-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^5.1.0",
    "mongoose": "^9.1.2",
    "multer": "^1.4.5-lts.1",
    "mongodb-memory-server": "^10.1.4"
  }
}
